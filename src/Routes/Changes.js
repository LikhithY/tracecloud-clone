import React from 'react'

const Changes = () => {
  return (
    <div style={{textAlign: 'center', marginTop: "10%"}}>
        These are the changes in project
    </div>
  )
}

export default Changes